version: '3.9'

services:
  postgres:
    image: postgres:latest
    container_name: webservices
    ports:
      - 5435:5432
    environment:
      - POSTGRES_PASSWORD=root
      - POSTGRES_USER=root
      - POSTGRES_DB=webservices